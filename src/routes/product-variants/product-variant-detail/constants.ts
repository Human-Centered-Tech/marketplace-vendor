export const VARIANT_DETAIL_FIELDS =
  // product.title feeds the breadcrumb + heading for single-variant products,
// whose stored variant title is the "Default variant" placeholder.
  "*inventory_items,*inventory_items.inventory,*inventory_items.inventory.location_levels,*options,*options.option,*prices,*prices.price_rules,product.title"
