// Replaced at container start by scripts/launch-vendor.js with the environment's
// backend URL and publishable API key. The default below keeps a plain
// `vite preview` / local static serve working before that script runs (the app
// falls back to its build-time values when these are empty).
window.__RUNTIME_CONFIG__ = window.__RUNTIME_CONFIG__ || {}
